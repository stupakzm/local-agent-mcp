---
phase: 04-configuration-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/parser.test.ts
  - src/__tests__/tools.test.ts
autonomous: true
gap_closure: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "npm run lint exits 0 with no errors"
    - "npm test exits 0 with all tests passing"
    - "No previously passing tests are broken by these changes"
  artifacts:
    - path: "src/__tests__/parser.test.ts"
      provides: "Lint-clean parser test suite"
      contains: "parseToolCall"
    - path: "src/__tests__/tools.test.ts"
      provides: "Lint-clean tools test suite with adequate timeout"
      contains: "kills grandchild processes on timeout"
  key_links: []
---

<objective>
Close 2 verification gaps from 04-VERIFICATION.md: (1) fix 7 lint errors in parser.test.ts and tools.test.ts so `npm run lint` exits 0, and (2) increase the timeout on the grandchild-kill test in tools.test.ts so `npm test` exits 0.

Purpose: Phase 4 acceptance criteria require both lint and test suite to pass cleanly. These gaps originate in pre-phase-4 test files but block phase completion.
Output: Both test files patched; `npm run lint` and `npm test` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-configuration-polish/04-VERIFICATION.md
@src/__tests__/parser.test.ts
@src/__tests__/tools.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lint errors in parser.test.ts and tools.test.ts</name>
  <files>src/__tests__/parser.test.ts, src/__tests__/tools.test.ts</files>
  <read_first>src/__tests__/parser.test.ts, src/__tests__/tools.test.ts</read_first>
  <action>
Fix 7 lint errors across two test files. All changes are safe removals of unused bindings — no logic changes.

**src/__tests__/parser.test.ts — 6 errors:**

1. **Line 3 — unused import `vi`:** Remove `vi` from the import. Change:
   `import { describe, it, expect, vi } from "vitest";`
   to:
   `import { describe, it, expect } from "vitest";`

2. **Line 14 — unused `_messages` parameter:** The function `async (_messages: OllamaMessage[])` has an unused parameter. Since this is a callback conforming to the `ChatFn` type, prefix with underscore is not enough for the lint rule. Add an eslint-disable-next-line comment above line 14:
   `// eslint-disable-next-line @typescript-eslint/no-unused-vars`
   OR remove the type annotation and use just `async (_messages)`. Check which approach the linter accepts — if `_messages` with underscore prefix is flagged, the eslint-disable comment is the correct fix since the parameter is required by the ChatFn type signature.

   Actually, looking more carefully: the `_messages` variables ARE prefixed with underscore, which typically suppresses unused-var warnings. If ESLint still flags them, add targeted `// eslint-disable-next-line @typescript-eslint/no-unused-vars` comments above each occurrence. The 5 occurrences are at lines 14, 21, 166, 267, 284.

   Specific lines to add the eslint-disable comment ABOVE:
   - Line 14: `return async (_messages: OllamaMessage[]): Promise<OllamaMessage> => {`
   - Line 21: `const noopChatFn: ChatFn = async (_messages: OllamaMessage[]) => {`
   - Line 166: `_messages: OllamaMessage[],`
   - Line 267: `_messages: OllamaMessage[],`
   - Line 284: `_messages: OllamaMessage[],`

**src/__tests__/tools.test.ts — 1 error:**

3. **Line 136 — useless assignment `alive`:** The variable `alive` is assigned on line 136 (`let alive = false`), then reassigned in the try/catch. The linter flags the initial assignment as useless because both branches of try/catch reassign it. Fix by removing the initial value:
   Change line 136 from:
   `let alive = false;`
   to:
   `let alive: boolean;`
   This tells TypeScript the type without a useless initial assignment. The try block sets `alive = true` and the catch block sets `alive = false`, so the variable is always assigned before the `expect(alive).toBe(false)` on line 144.
  </action>
  <verify>
    <automated>cd /home/stupakzm/projects/local-agent-mcp && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits with code 0
    - `grep -c "vi" src/__tests__/parser.test.ts` on line 3 import does NOT contain `vi` as a named import (or if _messages is the issue, eslint-disable comments are present above lines 14, 21, 166, 267, 284)
    - `grep "let alive: boolean" src/__tests__/tools.test.ts` returns a match (or equivalent fix that removes the useless assignment)
    - All test logic remains unchanged — no behavioral modifications
  </acceptance_criteria>
  <done>All 7 lint errors resolved. `npm run lint` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test timeout for grandchild-kill test in tools.test.ts</name>
  <files>src/__tests__/tools.test.ts</files>
  <read_first>src/__tests__/tools.test.ts</read_first>
  <action>
The test "kills grandchild processes on timeout" at line 114 of src/__tests__/tools.test.ts times out because the default vitest timeout is 5000ms, but this test needs more time: it spawns a bash process with a 500ms timeout, waits 200ms for OS cleanup, then checks the grandchild PID.

Add a timeout option to the test. Change line 114 from:
```
it.skipIf(process.platform === "win32")("kills grandchild processes on timeout", async () => {
```
to:
```
it.skipIf(process.platform === "win32")("kills grandchild processes on timeout", { timeout: 15000 }, async () => {
```

Use 15000ms (15 seconds) to provide ample headroom for slow CI/sandbox environments where process group kill propagation can take several seconds. The test itself only needs ~1-2 seconds in normal conditions, but environments with high load may need more.
  </action>
  <verify>
    <automated>cd /home/stupakzm/projects/local-agent-mcp && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0 (all tests pass)
    - `grep "timeout: 15000" src/__tests__/tools.test.ts` returns a match on the grandchild-kill test line
    - All other tests continue to pass (no regressions)
  </acceptance_criteria>
  <done>Test suite passes completely. `npm test` exits 0 with no timeouts or failures.</done>
</task>

</tasks>

<verification>
Both gaps from 04-VERIFICATION.md are closed:
1. Gap 1 (lint): `npm run lint` exits 0
2. Gap 2 (test timeout): `npm test` exits 0

Run both commands sequentially to confirm:
```bash
npm run lint && npm test
```
Both must exit 0.
</verification>

<success_criteria>
- `npm run lint` exits 0 with zero errors
- `npm test` exits 0 with all tests passing
- No test logic was changed — only lint suppressions and timeout adjustment
- Phase 4 acceptance criteria are now fully met
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-polish/04-03-SUMMARY.md`
</output>
